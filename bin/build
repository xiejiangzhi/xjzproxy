#! /usr/bin/env bash

set -ex

root="$(pwd)"

if [[ "$1" == "init_docker" ]]; then
  ./builder/init_docker
fi

./builder/compile
./builder/package
[[ $(which docker) ]] && ./builder/system_package

echo "Finished all"
# echo "Run system_package to build package for different system"
