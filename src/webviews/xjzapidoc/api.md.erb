#### <%= sub_title_icon + toc_id(api['title'] || api['path'], [:api, api.object_id]) %>

<%= api['desc'] %>

% if project.grpc
[GRPC Info](#<%= api['path'] %>)
% end

% if api['labels'].present?
**Labels**

<%= api['labels'].map { |name| "`#{name}`" }.join(' - ') %>
% end


**<%= api_req_icon %>Request**

```
<%= api['method'].to_s.upcase %> <%= api['path'] %>
```

% %w{query body params}.each do |name|
% if api[name].present?
**<%= name.titleize %>**

<%= render 'xjzapidoc/_data.md', data: api[name] %>
% end
% end

% api['response'].each do |name, res|
% next if res.blank? || res[0] == '.'
**<%= api_res_icon + name.titleize %> Response<%= id_tag("api_res_#{api['.index']}_#{name}") %>**

<%= render 'xjzapidoc/_data.md', data: res %>

Example

```json
<%= render_project_data('apis', [api['.index'], name]) %>
```
% end

