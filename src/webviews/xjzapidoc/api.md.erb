### <%= api['title'] || api['path'] %>

<%= api['desc'] %>

% if project.grpc
[GRPC Info](#<%= api['path'] %>)
% end

% if api['labels'].present?
**Labels**

<%= api['labels'].map { |name| "`#{name}`" }.join(' - ') %>
% end


**Request**

```
<%= api['method'].to_s.upcase %> <%= api['path'] %>
```

% %w{query body params}.each do |name|
% if api[name].present?
**<%= name.titleize %>**

<%= render 'xjzapidoc/_data.md', data: api[name] %>
% end
% end

% api['response'].each do |name, res|
% next if res.blank?
**<%= name.titleize %> Response**

<%= render 'xjzapidoc/_data.md', data: res %>

Example

```
<%= render_project_data('apis', [host_regexp, api['method'], api['path'], name]) %>
```
% end

