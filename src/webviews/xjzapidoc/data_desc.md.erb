|Name|Type|Description|Value|
|----|----|-----------|-------|
% format_data(data).each do |k, opts|
%   t = opts['type']
%   type_oper = opts['type_oper']
%   type = if type_oper == '*' 
%     "array of [#{md_escape(t)}](##{t})"
%   else
%     t.to_s =~ /^\.\w\// ? "[#{md_escape(t)}](##{get_ref_id(t)})" : t
%   end
%   val = opts['val']
%   k, type, opts, val = yield(k, type, opts, val) if block_given?
|<%= k -%>
|<%= type -%>
|<%= opts['desc'] -%>
|<%= val ? "`#{val.inspect}`" : '' %>|
% end
