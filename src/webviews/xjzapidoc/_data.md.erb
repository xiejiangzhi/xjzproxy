|Name|Type|Required|Description|Example|
|----|----|--------|-----------|-------|
% format_data(data).each do |k, opts|
%   t = opts['type']
%   type_oper = opts['type_oper']
%   type = if type_oper == '*' 
%     "array of [#{md_escape(t)}](##{t})"
%   else
%     t.to_s =~ /^\.\w\// ? "[#{md_escape(t)}](##{t})" : t
%   end
%   val = opts['val']
|<%= k -%>
|<%= type -%>
|<%= opts['optional'] != true -%>
|<%= opts['desc'] -%>
|<%= val ? "`#{val.inspect}`" : '' %>|
% end
