#### <%= icon_tag('list-alt') + toc_id(name.titleize, [:res]) %>

<%= response['desc'] %>

HTTP Code: **<%= response['http_code'] || 200 %>**

% if response['headers']
HTTP Headers

<%= render 'xjzapidoc/_data.md', data: response['headers'] %>
% end

HTTP Body

<%= render 'xjzapidoc/_data.md', data: response['data'] %>

Example

% if String === response['data']
```
<%= response['data'] %>
```
% else
```json
<%= render_project_data('responses', name) %>
```
% end
