$(document).ready(function() {
  var ws_url = 'ws://<%= $config.shared_data.app.server.ui_addr %>/ws';
  var xjz_view = new XjzView(ws_url, 'body');
  window.xjz_view = xjz_view;
  new ClipboardJS('.btn');

  $('[data-toggle="tooltip"]').tooltip()
})

